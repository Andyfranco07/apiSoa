import dotenv from 'dotenv';

dotenv.config();

export const config = {
  port: 3000, 
  dbUser: 'root',
  dbPassword: '1234',
  dbHost: '18.215.206.23',
  dbName: 'myapp',
  dbPort: 3306,
};
